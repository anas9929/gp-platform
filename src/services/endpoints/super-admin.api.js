import api from '@/services/api'

/**
 * نقاط النهاية الخاصة بلوحة: super-admin
 * تُضاف دالة لكل صفحة وقت وصفها.
 */
export default {
  // مثال:
  // getDashboardStats: () => api.get('/super-admin/dashboard/stats'),
}
