import api from '@/services/api'

/**
 * نقاط النهاية الخاصة بلوحة: team-leader
 * تُضاف دالة لكل صفحة وقت وصفها.
 */
export default {
  // مثال:
  // getDashboardStats: () => api.get('/team-leader/dashboard/stats'),
}
