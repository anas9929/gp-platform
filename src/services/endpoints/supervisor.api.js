import api from '@/services/api'

/**
 * نقاط النهاية الخاصة بلوحة: supervisor
 * تُضاف دالة لكل صفحة وقت وصفها.
 */
export default {
  // مثال:
  // getDashboardStats: () => api.get('/supervisor/dashboard/stats'),
}
