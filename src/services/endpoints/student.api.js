import api from '@/services/api'

/**
 * نقاط النهاية الخاصة بلوحة: student
 * تُضاف دالة لكل صفحة وقت وصفها.
 */
export default {
  // مثال:
  // getDashboardStats: () => api.get('/student/dashboard/stats'),
}
