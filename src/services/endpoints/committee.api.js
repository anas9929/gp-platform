import api from '@/services/api'

/**
 * نقاط النهاية الخاصة بلوحة: committee
 * تُضاف دالة لكل صفحة وقت وصفها.
 */
export default {
  // مثال:
  // getDashboardStats: () => api.get('/committee/dashboard/stats'),
}
